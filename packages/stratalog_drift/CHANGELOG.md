# 0.2.0

- `LoggerQueryInterceptor.maxStatementChars` now defaults to `null` — full SQL
  stays copyable from the log. Pass a value to opt back into clipping.

# 0.1.0

Initial release: Drift integration for stratalog.
