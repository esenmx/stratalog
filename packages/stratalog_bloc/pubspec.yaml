name: stratalog_bloc
description: BlocObserver for stratalog — bloc/cubit lifecycles, transitions, and failures into the State log layer.
version: 0.1.0
homepage: https://mehmetesen.com
repository: https://github.com/esenmx/stratalog
issue_tracker: https://github.com/esenmx/stratalog/issues
resolution: workspace

topics:
  - logging
  - bloc
  - state-management
  - observability

environment:
  sdk: ^3.11.0

dependencies:
  bloc: ^9.2.1
  stratalog: ^0.1.0

dev_dependencies:
  checks: ^0.3.1
  chirp: ^0.9.0
  test: ^1.30.0
  very_good_analysis: ^10.2.0
