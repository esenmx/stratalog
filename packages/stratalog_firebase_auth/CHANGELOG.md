# 0.2.0

- **Breaking:** the sign-in record no longer carries `email`, and `maskEmail` is removed — a masked address still identifies the person through its domain. The uid and provider ids are the identity.

# 0.1.0

Initial release: FirebaseAuth tap for stratalog.
